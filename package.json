{
  "name": "multipage-frontend-starter",
  "version": "0.0.1",
  "description": "A setup for frontend development with multiple javascript entrypoints and style bundling.",
  "main": "index.js",
  "scripts": {
    "build": "gulp --gulpfile tasks",
    "clean": "gulp clean --gulpfile tasks",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Maarten Flippo",
  "license": "MIT",
  "devDependencies": {
    "@babel/core": "^7.4.3",
    "@babel/preset-env": "^7.4.3",
    "babel-loader": "^8.0.5",
    "del": "^4.1.0",
    "gulp": "^4.0.0",
    "gulp-mode": "^1.0.2",
    "webpack": "^4.30.0"
  }
}
